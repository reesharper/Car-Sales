

const initialState = {

}

const reducer = (state, action) => {
  switch(action.type) {

    default:
    return state;
  }
};

const reducerExport = { initialState, reducer }
export default reducerExport